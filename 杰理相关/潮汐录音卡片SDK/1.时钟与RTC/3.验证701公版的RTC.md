# 验证 701 公版的 RTC

> 目标：通过写测试模块 + 看日志，验证公版硬件 RTC 是否正确走时，覆盖正常运行、深度睡眠、软关机三个场景。

---

## 一、使能 RTC

### 1.1 开启宏

`apps/earphone/board/br28/board_ac701n_demo_cfg.h`：

```c
#define TCFG_APP_RTC_EN         1       // 0 → 1，开启 RTC
```

### 1.2 确认时钟源

同文件（第 542 行左右）：

```c
#define RTC_CLK_RES_SEL         CLK_SEL_32K
```

| 情况 | 配置 |
|---|---|
| 板上焊了 32.768 kHz 小晶振（接 RTC_OSC 引脚） | 保持 `CLK_SEL_32K`，精度最高 |
| 板上**没有焊** 32K 晶振（成本裁剪） | 改为 `CLK_SEL_LRC`，否则 RTC 无法走时 |

### 1.3 确认设备注册（通常不用改）

`apps/earphone/device_config.c`，`TCFG_APP_RTC_EN` 打开后以下代码自动生效：

```c
const struct sys_time def_sys_time = {
    .year = 2020, .month = 1, .day = 1,
    .hour = 0,    .min  = 0, .sec  = 0,
};
const struct sys_time def_alarm = {
    .year = 2050, .month = 1, .day = 1,
    .hour = 0,    .min  = 0, .sec  = 0,
};

RTC_DEV_PLATFORM_DATA_BEGIN(rtc_data)
    .default_sys_time = &def_sys_time,
    .default_alarm    = &def_alarm,
    .rtc_clk          = RTC_CLK_RES_SEL,
    .rtc_sel          = HW_RTC,
    .cbfun            = NULL,            // 注册闹钟回调时改为 rtc_test_alarm_callback
RTC_DEV_PLATFORM_DATA_END()
```

---

## 二、测试模块文件

测试代码分为两个文件，直接新建放入 SDK 中：

| 文件 | 路径 |
|---|---|
| 头文件 | `apps/earphone/include/rtc_test.h` |
| 实现文件 | `apps/earphone/rtc_test.c` |

---

### 2.1 头文件 `rtc_test.h`

```c
/*===========================================================================
 * FILE: rtc_test.h
 * DESC: 公版硬件 RTC 测试模块头文件
 *
 * 使用前提：
 *   board_ac701n_demo_cfg.h 中 TCFG_APP_RTC_EN = 1
 *
 * 调用说明（三处）：
 *
 *   ① apps/earphone/app_main.c  →  app_task_init() 末尾
 *        rtc_test_init();
 *
 *   ② apps/earphone/device_config.c  →  RTC 平台数据的 cbfun 字段
 *        .cbfun = rtc_test_alarm_callback,
 *
 *   ③ apps/earphone/mode/bt/poweroff.c  →  sys_enter_soft_poweroff() 开头
 *        rtc_test_softoff_prepare();
 *===========================================================================*/

#ifndef __RTC_TEST_H__
#define __RTC_TEST_H__

#if 1//TCFG_APP_RTC_EN

#include "typedef.h"

/*
 * rtc_test_init()
 *   每次上电后调用一次。
 *   - 立即执行：寄存器检查、读写验证、工具函数验证、Soft-Off 连续性打印
 *   - 后台执行：60 秒走时精度测试（每 10s 打印一次）
 *               20 秒闹钟触发测试
 */
void rtc_test_init(void);

/*
 * rtc_test_softoff_prepare()
 *   在软关机流程入口处调用，打印关机前的 RTC 时间，
 *   供下次上电时对比时间是否连续。
 */
void rtc_test_softoff_prepare(void);

/*
 * rtc_test_alarm_callback(u8 reason)
 *   闹钟中断回调，注册到 device_config.c 的 .cbfun 字段。
 *   闹钟触发时由 RTC 驱动自动调用。
 */
void rtc_test_alarm_callback(u8 reason);

#endif /* TCFG_APP_RTC_EN */

#endif /* __RTC_TEST_H__ */
```

---

### 2.2 实现文件 `rtc_test.c`

```c
/*===========================================================================
 * FILE: rtc_test.c
 * DESC: 公版硬件 RTC 综合测试模块
 *
 * 场景覆盖：
 *   Test1  寄存器原始值转储  （rtc_debug_dump）
 *   Test2  基础读写验证       （rtc_write_time / rtc_read_time）
 *   Test3  60 秒走时精度      （每 10 s 采样 × 6，后台定时器）
 *   Test4  20 秒闹钟触发      （rtc_write_alarm + rtc_alarm_en + 回调）
 *   Test5  工具函数验证       （time_diff_for_sec / time_add_sec /
 *                               caculate_weekday_by_time / 月末跨界 /
 *                               年末跨界 / datetime_to_sec↔sec_to_datetime）
 *   Test6  Soft-Off 连续性    （每次上电打印 RTC；关机前打印关机时刻）
 *===========================================================================*/

#include "rtc_test.h"
#include "board_ac701n_demo_cfg.h"

#if 1//TCFG_APP_RTC_EN

#include "rtc/rtc_dev.h"
#include "system/timer.h"
#include "system/generic/jiffies.h"

/* -------------------------------------------------------------------------
 * 内部日志宏
 * ---------------------------------------------------------------------- */
#define RTC_LOG(fmt, ...)  printf("[RTC_TEST] " fmt "\n", ##__VA_ARGS__)

/* =========================================================================
 * Test1: 寄存器原始值转储
 *   目的：确认 RTC 外设已初始化、寄存器不全为 0
 *   预期：rtc_debug_dump() 打印非零的寄存器内容
 * ======================================================================= */
static void test1_reg_dump(void)
{
    RTC_LOG("=== Test1: Register Dump ===");
    rtc_debug_dump();//这里日志打印的是设定的初始时间
    RTC_LOG("=== Test1 Done ===");
}

/* =========================================================================
 * Test2: 基础读写验证
 *   目的：确认 rtc_write_time / rtc_read_time 接口通路正常
 *   预期日志（正常）：
 *     [RTC_TEST] write:     2025-06-15 10:30:00
 *     [RTC_TEST] read back: 2025-06-15 10:30:00
 *     [RTC_TEST] Test2 PASS: read back matches write
 *   异常：
 *     FAIL → TCFG_APP_RTC_EN 未生效，或 RTC 硬件故障
 * ======================================================================= */
static void test2_basic_rw(void)
{
    struct sys_time set_t = {
        .year = 2025, .month = 6, .day = 15,
        .hour = 10,   .min   = 30, .sec = 0,
    };
    struct sys_time rb = {0};

    RTC_LOG("=== Test2: Basic Read/Write ===");

    rtc_write_time(&set_t);
    /*RTC_LOG("write:     %04d-%02d-%02d %02d:%02d:%02d",
            set_t.year, set_t.month, set_t.day,
            set_t.hour, set_t.min,   set_t.sec);*/

    rtc_read_time(&rb);
    /*RTC_LOG("read back: %04d-%02d-%02d %02d:%02d:%02d",
            rb.year, rb.month, rb.day, rb.hour, rb.min, rb.sec);*/

    if (rb.year  == set_t.year  &&
        rb.month == set_t.month &&
        rb.day   == set_t.day   &&
        rb.hour  == set_t.hour  &&
        rb.min   == set_t.min) {
        RTC_LOG("Test2 PASS: read back matches write");
    } else {
        RTC_LOG("Test2 FAIL: mismatch! Check TCFG_APP_RTC_EN or RTC hardware");
    }
    RTC_LOG("=== Test2 Done ===");
}

/* =========================================================================
 * Test3: 60 秒走时精度验证（每 10 秒采样一次，共 6 次）
 *   目的：验证 RTC 实际在走时，且走时速度与系统 jiffies 基本一致
 *   预期日志（正常）：
 *     [RTC_TEST] Test3 [1/6] RTC now: 2025-06-15 10:30:10
 *       jiffies elapsed: ~10 s  |  RTC elapsed: 10 s
 *       -> PASS (expected ~10 s)
 *     ... (重复 6 次，最后一次打印 Done)
 *   异常排查：
 *     RTC elapsed: 0 s → 时钟源不通（无 32K 晶振则改 CLK_SEL_LRC）
 *     elapsed 远超预期  → 寄存器数据异常，查看 Test1 dump
 *     走时明显偏快/慢   → CLK_SEL_LRC 温漂，换外部 32K 晶振
 * ======================================================================= */
static struct sys_time  s_t3_start_rtc = {0};
static unsigned long    s_t3_start_ms  = 0;
static int              s_t3_count     = 0;
static int              s_t3_timer_id  = 0;

static void test3_sample(void *priv)
{
    struct sys_time now = {0};

    s_t3_count++;
    rtc_read_time(&now);

    u32 elapsed_jiffies_s = (unsigned long)(jiffies_msec() - s_t3_start_ms) / 1000;
    u32 elapsed_rtc_s     = time_diff_for_sec(&s_t3_start_rtc, &now);
    u32 expected          = (u32)s_t3_count * 10;

    RTC_LOG("Test3 [%d/6] RTC now: %04d-%02d-%02d %02d:%02d:%02d",
            s_t3_count,
            now.year, now.month, now.day, now.hour, now.min, now.sec);
    RTC_LOG("  jiffies elapsed: ~%u s  |  RTC elapsed: %u s",
            elapsed_jiffies_s, elapsed_rtc_s);

    if (elapsed_rtc_s == 0) {
        RTC_LOG("  -> FAIL: RTC NOT ticking. Check clock source (CLK_SEL_32K needs crystal)");
    } else if (elapsed_rtc_s >= expected - 2 && elapsed_rtc_s <= expected + 2) {
        RTC_LOG("  -> PASS (expected ~%u s)", expected);
    } else {
        RTC_LOG("  -> WARN: RTC %u s vs expected ~%u s", elapsed_rtc_s, expected);
    }

    if (s_t3_count >= 6) {
        sys_timer_del(s_t3_timer_id);
        s_t3_timer_id = 0;
        RTC_LOG("=== Test3 Done (60s timing accuracy complete) ===");
    }
}

static void test3_timing_start(void)
{
    RTC_LOG("=== Test3: 60s Timing Accuracy (sample every 10s x 6) ===");
    rtc_read_time(&s_t3_start_rtc);
    s_t3_start_ms = (unsigned long)jiffies_msec();
    s_t3_count    = 0;

    RTC_LOG("Test3 start: %04d-%02d-%02d %02d:%02d:%02d",
            s_t3_start_rtc.year, s_t3_start_rtc.month, s_t3_start_rtc.day,
            s_t3_start_rtc.hour, s_t3_start_rtc.min,   s_t3_start_rtc.sec);

    s_t3_timer_id = sys_timer_add(NULL, test3_sample, 10 * 1000);
}

/* =========================================================================
 * Test4: 20 秒闹钟触发验证
 *   目的：验证 rtc_write_alarm + rtc_alarm_en + 回调链路
 *   预期日志（正常，约 20 秒后）：
 *     [RTC_TEST] === Test4: Alarm Callback Fired! reason=0x01 ===
 *     [RTC_TEST]   RTC at alarm: 2025-06-15 10:30:20
 *     [RTC_TEST] Test4 PASS: alarm fired and disabled
 *   异常排查：
 *     20 秒后无回调 → device_config.c 的 .cbfun 未注册为 rtc_test_alarm_callback
 *                     或 rtc_alarm_en(1) 未调用
 *     时间不对       → 检查 time_add_sec 跨界逻辑
 * ======================================================================= */
void rtc_test_alarm_callback(u8 reason)
{
    struct sys_time fired = {0};
    rtc_read_time(&fired);

    RTC_LOG("=== Test4: Alarm Callback Fired! reason=0x%02X ===", reason);
    RTC_LOG("  RTC at alarm: %04d-%02d-%02d %02d:%02d:%02d",
            fired.year, fired.month, fired.day,
            fired.hour, fired.min,   fired.sec);

    rtc_alarm_en(0);    /* 关闭闹钟，防止持续触发 */
    RTC_LOG("Test4 PASS: alarm fired and disabled");
}

static void test4_alarm_start(void)
{
    struct sys_time now   = {0};
    struct sys_time alarm = {0};

    RTC_LOG("=== Test4: 20s Alarm Trigger ===");

    rtc_read_time(&now);
    copy_time(&now, &alarm);
    time_add_sec(&alarm, 20);   /* 当前时间 + 20 秒 = 闹钟时刻 */

    rtc_write_alarm(&alarm);
    rtc_alarm_en(1);

    RTC_LOG("Test4 now:   %04d-%02d-%02d %02d:%02d:%02d",
            now.year,   now.month,   now.day,   now.hour,   now.min,   now.sec);
    RTC_LOG("Test4 alarm: %04d-%02d-%02d %02d:%02d:%02d  (fires in ~20s)",
            alarm.year, alarm.month, alarm.day, alarm.hour, alarm.min, alarm.sec);
}

/* =========================================================================
 * Test5: 工具函数验证
 *   目的：对 rtc_dev.h 提供的工具函数进行单元测试
 *   覆盖：time_diff_for_sec / time_add_sec (正向跨分钟 / 月末 / 年末) /
 *         caculate_weekday_by_time / datetime_to_sec↔sec_to_datetime /
 *         month_to_day 闰年判断
 * ======================================================================= */
static void test5_utils(void)
{
    RTC_LOG("=== Test5: Utility Functions ===");

    //这一些花括号的打印没有

    /* 5a: time_diff_for_sec —— 基础差值 */
    {
        struct sys_time ta = {.year=2025,.month=6,.day=15,.hour=10,.min=0,.sec=0};
        struct sys_time tb = {.year=2025,.month=6,.day=15,.hour=10,.min=1,.sec=30};
        u32 diff = time_diff_for_sec(&ta, &tb);
        RTC_LOG("5a time_diff_for_sec: expected 90, got %u -> %s",
                diff, diff == 90 ? "PASS" : "FAIL");
    }

    /* 5b: time_add_sec —— 正向跨分钟 */
    {
        struct sys_time t = {.year=2025,.month=6,.day=15,.hour=10,.min=59,.sec=45};
        time_add_sec(&t, 20);   /* 10:59:45 + 20s = 11:00:05 */
        int ok = (t.hour==11 && t.min==0 && t.sec==5);
        RTC_LOG("5b time_add_sec cross-minute: expected 11:00:05, got %02d:%02d:%02d -> %s",
                t.hour, t.min, t.sec, ok ? "PASS" : "FAIL");
    }

    /* 5c: time_add_sec —— 月末跨界（6月→7月） */
    {
        struct sys_time t = {.year=2025,.month=6,.day=30,.hour=23,.min=59,.sec=50};
        time_add_sec(&t, 15);   /* 2025-06-30 23:59:50 + 15s = 2025-07-01 00:00:05 */
        int ok = (t.month==7 && t.day==1 && t.hour==0 && t.min==0 && t.sec==5);
        RTC_LOG("5c time_add_sec cross-month: expected 2025-07-01 00:00:05, "
                "got %04d-%02d-%02d %02d:%02d:%02d -> %s",
                t.year, t.month, t.day, t.hour, t.min, t.sec, ok ? "PASS" : "FAIL");
    }

    /* 5d: time_add_sec —— 年末跨界 */
    {
        struct sys_time t = {.year=2025,.month=12,.day=31,.hour=23,.min=59,.sec=55};
        time_add_sec(&t, 10);   /* 2025-12-31 23:59:55 + 10s = 2026-01-01 00:00:05 */
        int ok = (t.year==2026 && t.month==1 && t.day==1 &&
                  t.hour==0   && t.min==0   && t.sec==5);
        RTC_LOG("5d time_add_sec cross-year: expected 2026-01-01 00:00:05, "
                "got %04d-%02d-%02d %02d:%02d:%02d -> %s",
                t.year, t.month, t.day, t.hour, t.min, t.sec, ok ? "PASS" : "FAIL");
    }

    /* 5e: caculate_weekday_by_time */
    {
        struct sys_time t = {.year=2025,.month=6,.day=15};  /* 周日 */
        u8 wd = caculate_weekday_by_time(&t);
        RTC_LOG("5e weekday 2025-06-15(Sun): expected 0, got %d -> %s",
                wd, wd == 0 ? "PASS" : "FAIL");

        t.day = 16;  /* 周一 */
        wd = caculate_weekday_by_time(&t);
        RTC_LOG("5e weekday 2025-06-16(Mon): expected 1, got %d -> %s",
                wd, wd == 1 ? "PASS" : "FAIL");
    }

    /* 5f: datetime_to_sec + sec_to_datetime 往返验证 */
    {
        struct sys_time orig = {.year=2025,.month=6,.day=15,.hour=12,.min=30,.sec=45};
        struct sys_time back = {0};
        u64 secs = datetime_to_sec(&orig);
        sec_to_datetime(secs, &back);
        int ok = (back.year ==orig.year  && back.month==orig.month &&
                  back.day  ==orig.day   && back.hour ==orig.hour  &&
                  back.min  ==orig.min   && back.sec  ==orig.sec);
        RTC_LOG("5f datetime_to_sec+sec_to_datetime: %04d-%02d-%02d %02d:%02d:%02d "
                "-> %llu s -> %04d-%02d-%02d %02d:%02d:%02d -> %s",
                orig.year, orig.month, orig.day, orig.hour, orig.min, orig.sec,
                secs,
                back.year, back.month, back.day, back.hour, back.min, back.sec,
                ok ? "PASS" : "FAIL");
    }

    /* 5g: month_to_day —— 闰年 2 月验证 */
    {
        u16 d_leap   = month_to_day(2024, 2);   /* 2024 闰年：31+29 = 60 */
        u16 d_normal = month_to_day(2025, 2);   /* 2025 平年：31+28 = 59 */
        RTC_LOG("5g month_to_day: 2024-Feb=%d(exp 60)%s  2025-Feb=%d(exp 59)%s",
                d_leap,   d_leap   == 60 ? " PASS" : " FAIL",
                d_normal, d_normal == 59 ? " PASS" : " FAIL");
    }

    RTC_LOG("=== Test5 Done ===");
}

/* =========================================================================
 * Test6: Soft-Off 连续性验证（人工对比两次日志）
 *
 *   操作步骤：
 *     1. 上电 → 自动打印 [Boot RTC]（rtc_test_init 中调用 test6_boot_print）
 *     2. 等待若干秒
 *     3. 触发软关机 → 自动打印 [PowerOff RTC]
 *     4. 等待 N 秒（模拟放入充电盒）
 *     5. 重新上电 → 自动打印 [Boot RTC]
 *     6. 人工对比：Boot RTC 是否比上一次 PowerOff RTC 推后了约 N 秒
 *
 *   预期（正常）：
 *     [RTC_TEST] Boot RTC:     2025-06-15 10:30:00
 *     [RTC_TEST] PowerOff RTC: 2025-06-15 10:30:05
 *     ... 软关机 30 秒 ...
 *     [RTC_TEST] Boot RTC:     2025-06-15 10:30:35  ← 时间连续 ✓
 *
 *   异常：
 *     Boot RTC == 2020-01-01 00:00:00  → P33 RTCVDD 断电，RTC 寄存器丢失
 *     Boot RTC == PowerOff RTC         → RTC 在 Soft-Off 期间停振
 *                                        (检查时钟源 / 32K 晶振焊接情况)
 * ======================================================================= */
static void test6_boot_print(void)
{
    struct sys_time now = {0};
    rtc_read_time(&now);

    RTC_LOG("=== Test6: Soft-Off Continuity ===");

    //无日志打印
    RTC_LOG("Boot RTC: %04d-%02d-%02d %02d:%02d:%02d",
            now.year, now.month, now.day, now.hour, now.min, now.sec);
    RTC_LOG("  -> Compare with last [PowerOff RTC] to verify continuity.");
    RTC_LOG("  -> 2020-01-01 00:00:00 means P33 RTCVDD lost power (RTC registers reset).");
    RTC_LOG("  -> Unchanged vs PowerOff means RTC stopped during Soft-Off.");
    //无日志打印

    RTC_LOG("=== Test6 Boot Done ===");
}

void rtc_test_softoff_prepare(void)
{
    struct sys_time now = {0};
    rtc_read_time(&now);
    RTC_LOG("=== Test6: PowerOff RTC: %04d-%02d-%02d %02d:%02d:%02d ===",
            now.year, now.month, now.day, now.hour, now.min, now.sec);
    RTC_LOG("  -> Next boot [Boot RTC] should be that many seconds ahead.");
}

/* =========================================================================
 * 对外接口：rtc_test_init()
 *   在 app_task_init() 末尾调用一次。
 *   立即执行：Test1 / Test2 / Test5 / Test6
 *   后台执行：Test3（每 10s，共 60s）、Test4（20s 后触发闹钟回调）
 * ======================================================================= */
void rtc_test_init(void)
{
    RTC_LOG("========================================");
    RTC_LOG("  RTC Test Module Start");
    RTC_LOG("========================================");

    test1_reg_dump();       /* 立即：寄存器原始值 */
    //test2_basic_rw();       /* 立即：读写验证 */
    //test5_utils();          /* 立即：工具函数验证 */
    //test6_boot_print();     /* 立即：本次上电 RTC 时间（Soft-Off 连续性参考） */
    //test4_alarm_start();    /* 立即设闹钟：20s 后触发回调 */
    test3_timing_start();   /* 后台：每 10s 采样，共 60s */

    RTC_LOG("Init done. Test3(60s timing) & Test4(20s alarm) running in background.");
    RTC_LOG("========================================");
}

#endif /* TCFG_APP_RTC_EN */
```

---

## 三、集成到 SDK（三处 + Makefile）

### 3.1 注册编译文件

`build/Makefile.mk`，在 `c_SRC_FILES` 中 `apps/earphone/app_main.c \` 下方追加一行：

```makefile
c_SRC_FILES := \
    ...
    apps/earphone/app_main.c \
    apps/earphone/rtc_test.c \      ← 新增这一行
    apps/earphone/audio/jlstream_event_handler.c \
    ...
```

---

### 3.2 调用点 ①：上电初始化

文件：`apps/earphone/app_main.c`

**第一步**：在文件顶部 include 区域末尾加入头文件：

```c
#include "vm.h"
#include "rtc_test.h"    // ← 新增
```

**第二步**：在 `app_task_init()` 函数末尾（`return mode;` 之前）调用：

```c
static struct app_mode *app_task_init()
{
    // ... 原有初始化代码 ...

    struct app_mode *mode;
    mode = app_mode_switch_handler(msg);
    ASSERT(mode != NULL);

#if TCFG_APP_RTC_EN
    rtc_test_init();        // ← 新增
#endif

    return mode;
}
```

---

### 3.3 调用点 ②：注册闹钟回调

文件：`apps/earphone/device_config.c`

**第一步**：加入头文件：

```c
#include "rtc/rtc_dev.h"
#include "rtc_test.h"       // ← 新增
```

**第二步**：将 `.cbfun = NULL` 改为：

```c
RTC_DEV_PLATFORM_DATA_BEGIN(rtc_data)
    .default_sys_time = &def_sys_time,
    .default_alarm    = &def_alarm,
    .rtc_clk          = RTC_CLK_RES_SEL,
    .rtc_sel          = HW_RTC,
    .cbfun            = rtc_test_alarm_callback,    // ← NULL 改为这个
RTC_DEV_PLATFORM_DATA_END()
```

---

### 3.4 调用点 ③：软关机前打印

文件：`apps/earphone/mode/bt/poweroff.c`

开TWS的在这个文件：`apps\earphone\mode\bt\tws_poweroff.c`

**第一步**：加入头文件：

```c
#include "poweroff.h"
#include "bt_background.h"
#include "rtc_test.h"       // ← 新增
```

**第二步**：在 `sys_enter_soft_poweroff()` 函数开头加入（紧接 log_info 之后）：

```c
void sys_enter_soft_poweroff(enum poweroff_reason reason)
{
    log_info("sys_enter_soft_poweroff: %d\n", reason);

#if TCFG_APP_RTC_EN
    rtc_test_softoff_prepare();     // ← 新增
#endif

    // ... 原有代码继续 ...
}
```

---

## 四、测试场景说明

| 编号 | 场景 | 执行时机 | 验证内容 |
|---|---|---|---|
| Test1 | 寄存器转储 | 上电立即 | `rtc_debug_dump()` 输出非零 → 外设已初始化 |
| Test2 | 基础读写 | 上电立即 | 写入已知时间，读回年月日时分一致 |
| Test3 | 60s 走时精度 | 后台，每 10s × 6 次 | RTC elapsed 与 jiffies elapsed 误差 ≤ 2s |
| Test4 | 20s 闹钟 | 后台，约 20s 后触发回调 | 完整链路：write_alarm → alarm_en → cbfun → 关闭 |
| Test5 | 工具函数 | 上电立即 | `time_diff`、`time_add_sec`（跨分/月/年）、weekday、datetime↔sec、闰年 |
| Test6 | Soft-Off 连续性 | 上电打印 + 关机前打印 | 人工对比两次日志，验证 P33 RTCVDD 域保电 |

---

## 五、预期日志（正常情况）

```
[RTC_TEST] ========================================
[RTC_TEST]   RTC Test Module Start
[RTC_TEST] ========================================

[RTC_TEST] === Test1: Register Dump ===
  (rtc_debug_dump 打印若干寄存器值，非全 0)
[RTC_TEST] === Test1 Done ===

[RTC_TEST] === Test2: Basic Read/Write ===
[RTC_TEST] write:     2025-06-15 10:30:00
[RTC_TEST] read back: 2025-06-15 10:30:00
[RTC_TEST] Test2 PASS: read back matches write
[RTC_TEST] === Test2 Done ===

[RTC_TEST] === Test5: Utility Functions ===
[RTC_TEST] 5a time_diff_for_sec: expected 90, got 90 -> PASS
[RTC_TEST] 5b time_add_sec cross-minute: expected 11:00:05, got 11:00:05 -> PASS
[RTC_TEST] 5c time_add_sec cross-month: expected 2025-07-01 00:00:05, got ... -> PASS
[RTC_TEST] 5d time_add_sec cross-year:  expected 2026-01-01 00:00:05, got ... -> PASS
[RTC_TEST] 5e weekday 2025-06-15(Sun): expected 0, got 0 -> PASS
[RTC_TEST] 5e weekday 2025-06-16(Mon): expected 1, got 1 -> PASS
[RTC_TEST] 5f datetime_to_sec+sec_to_datetime: ... -> PASS
[RTC_TEST] 5g month_to_day: 2024-Feb=60 PASS  2025-Feb=59 PASS
[RTC_TEST] === Test5 Done ===

[RTC_TEST] === Test6: Soft-Off Continuity ===
[RTC_TEST] Boot RTC: 2025-06-15 10:30:00
[RTC_TEST]   -> Compare with last [PowerOff RTC] to verify continuity.
[RTC_TEST] === Test6 Boot Done ===

[RTC_TEST] === Test4: 20s Alarm Trigger ===
[RTC_TEST] Test4 now:   2025-06-15 10:30:00
[RTC_TEST] Test4 alarm: 2025-06-15 10:30:20  (fires in ~20s)

[RTC_TEST] === Test3: 60s Timing Accuracy (sample every 10s x 6) ===
[RTC_TEST] Test3 start: 2025-06-15 10:30:00
[RTC_TEST] Init done. Test3(60s timing) & Test4(20s alarm) running in background.
[RTC_TEST] ========================================

  ... 约 20 秒后 ...

[RTC_TEST] === Test4: Alarm Callback Fired! reason=0x01 ===
[RTC_TEST]   RTC at alarm: 2025-06-15 10:30:20
[RTC_TEST] Test4 PASS: alarm fired and disabled

  ... 每 10 秒一次，共 6 次 ...

[RTC_TEST] Test3 [1/6] RTC now: 2025-06-15 10:30:10
[RTC_TEST]   jiffies elapsed: ~10 s  |  RTC elapsed: 10 s
[RTC_TEST]   -> PASS (expected ~10 s)
...
[RTC_TEST] Test3 [6/6] RTC now: 2025-06-15 10:31:00
[RTC_TEST]   jiffies elapsed: ~60 s  |  RTC elapsed: 60 s
[RTC_TEST]   -> PASS (expected ~60 s)
[RTC_TEST] === Test3 Done (60s timing accuracy complete) ===

  ... 触发软关机时 ...

[RTC_TEST] === Test6: PowerOff RTC: 2025-06-15 10:31:30 ===
[RTC_TEST]   -> Next boot [Boot RTC] should be that many seconds ahead.
```

---

## 六、常见问题排查

| 问题现象 | 最可能原因 | 解决方法 |
|---|---|---|
| Test2 FAIL，read back 全 0 | `TCFG_APP_RTC_EN` 未生效 | 确认宏值为 1，重新编译 |
| 读回始终是 `2020-01-01 00:00:00` | 每次上电都从默认值重置，RTC 未持久 | 检查 P33 RTCVDD 供电；无 32K 晶振改 `CLK_SEL_LRC` |
| Test3 `RTC elapsed: 0 s` | RTC 时钟源不通 | 无外部 32K 晶振时改 `CLK_SEL_LRC` |
| Test3 走时明显偏快或偏慢 | `CLK_SEL_LRC` 温漂 | 换用外部 32K 晶振 |
| Test4 20 秒后无回调 | `.cbfun` 未注册 | 确认 `device_config.c` 中 `.cbfun = rtc_test_alarm_callback` |
| Test1 `rtc_debug_dump()` 打印全 0 | RTC 外设未初始化 | 确认 `rtc_config_init` 已被调用（设备注册流程走通） |
| Test6 Boot RTC 回到 `2020-01-01` | P33 RTCVDD 断电，寄存器丢失 | 检查 VBAT 供电路径 |
| Test6 Boot RTC 与 PowerOff RTC 相同 | RTC 在 Soft-Off 期间停振 | 检查时钟源选择 / 32K 晶振焊接 |

- -test1可以读取到设置的默认时间
- -test2读写通过验证
- -test5打印丢失
- -test6连续性可以保证，但是精度暂时没有确认
- -test4直接丢失打印，闹钟没有触发对应的回调函数
- -test3通过验证
